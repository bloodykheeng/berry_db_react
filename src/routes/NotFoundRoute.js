import NotFound from 'components/NotFound';

// ==============================|| AUTHENTICATION ROUTING ||============================== //

const NotFoundRoute = {
    path: '*',
    element: <NotFound />
};

export default NotFoundRoute;
